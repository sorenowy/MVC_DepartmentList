@model IEnumerable<DepartmentApplication.Models.Department>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DepartmentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DepartmentName)
        </td>
        <td>
            @Html.ActionLink("Zmien", "Edit", new { id=item.DepartmentId }) |
            @Html.ActionLink("Szczegoly", "Details", new { id=item.DepartmentId }) |
            @Html.ActionLink("Usun", "Delete", new { id=item.DepartmentId })
        </td>
    </tr>
}

</table>
